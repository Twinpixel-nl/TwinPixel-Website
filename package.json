{
  "name": "twinpixel-website",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run generate && npm run sitemap",
    "generate": "node js/generate-blog.js",
    "sitemap": "node js/sitemapgenerator.js"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
