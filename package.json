{
  "name": "twinpixel-website",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run generate && npm run sitemap",
    "generate": "node js/sitegenerator.js",
    "sitemap": "node js/sitemap-generator.js"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
